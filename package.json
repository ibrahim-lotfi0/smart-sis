{
  "name": "smart-sis",
  "version": "1.0.0",
  "description": "Smart University Management System (SIS)",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npx serve -l 3000 frontend-html",
    "dev:backend": "npm run dev --prefix backend",
    "dev:ml": "npm run dev:ml --prefix backend",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev:ml --prefix backend\" \"npx serve -l 3000 frontend-html\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
